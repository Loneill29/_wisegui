<div class="form-group">
    <% if current_user.premium? || current_user.admin? %>
    <div>
      <hr>
      <h3>Add Collaborators</h3>
        <%= form_for [@wiki, Collaborator.new] do |c| %>
        <%= email_field_tag :collaborator %>
        <%= c.submit "Add collaborator", class: 'btn btn-default btn-collab' %>
    <% end %>
    <hr>
    <h3>Manage Collaborators</h3>
    <% wiki.collaborators.each do |c| %>
      <% user = User.find(c.user_id) %>
      <%= user.email %>
      <%= link_to "Delete", wiki_collaborator_path(@wiki, c), class: 'btn btn-delete', method: :delete%>
    <% end %>
  </div>
  <% end %>
</div>
